__pycache__/
*.pyc
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.pdf

# Secrets — never commit
.env
.env.*
!.env.example

# CTF lab: per-round scratch state, attacker-observable sinks, and answer keys —
# none of these should ship in the repo.
harness/lab/state/
harness/lab/sinks/
harness/lab/boxes/*/SOLUTION/

# Operator/answer-bearing docs — the CTF answer key; never ship to students.
docs/operator/
harness/lab/boxes/*/README.md
harness/lab/boxes/*/NOTES.md

# Runtime stores and local scratch — not for the repo.
harness/echoleak/runs/
harness/asi02-store/
harness/asi05-store/
err.log

# Local Claude Code project MCP config — stale/local; a shipped one would auto-wire vulnerable servers on clone.
.mcp.json
